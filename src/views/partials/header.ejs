<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MovieVault</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>

  <section class="header">
    <header class="p-3 border-bottom">
        <div class="container">
            <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 link-body-emphasis text-decoration-none">
                    <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap">
                        <use xlink:href="#bootstrap"></use>
                    </svg>
                </a>

                <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                    <li><a href="/" class="nav-link px-2 header-color">Home</a></li>
                    <li><a href="/favorites" class="nav-link px-2 header-color">Favorites</a></li>
                    <li><a href="/watchlist" class="nav-link px-2 header-color">Watchlist</a></li>
                </ul>                

                <form class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3 search-box d-flex" role="search" action="/search_movie" method="GET">
                    <input type="search" class="form-control me-2" placeholder="Search..." aria-label="Search" name="title" required>
                    <button type="submit" class="btn btn-outline-danger">Search</button>
                </form>

                <!-- Conditional Sign Up Button -->
                <div class="ml-auto">
                  <% if (!user) { %>
                      <a href="/login" class="btn btn-success me-3">Sign In</a>
                  <% } %>
                </div>

                <div class="dropdown text-end">
                    <a href="#" class="d-block link-body-emphasis text-decoration-none dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="/assets/profile.png" alt="mdo" width="32" height="32" class="rounded-circle">
                    </a>
                    <ul class="dropdown-menu text-small">
                        <li><a class="dropdown-item" href="/profile">Profile</a></li>
                        <li><hr class="dropdown-divider"></li>

                        <% if (user) { %>
                          <li><a class="dropdown-item" href="/auth/logout">Sign Out</a></li>
                        <% } %>
                        
                    </ul>
                </div>

            </div>
        </div>
    </header>
</section>

